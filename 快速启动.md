终端 1 — Java 模拟引擎：
cd /Users/h0pe_wsv/Desktop/FTC-3DPath-Simulator-main/FTC-Path-Simulator
javac -d out --release 17 -cp lib/gson-2.10.1.jar $(find src/main/java -name "*.java") && java -cp out:lib/gson-2.10.1.jar simulator.server.SimServer

终端 2 — Metal 渲染前端：
cd /Users/h0pe_wsv/Desktop/FTC-3DPath-Simulator-main/MetalRenderer
swift run

先启动终端 1，看到 Listening on port 9876 后再启动终端 2。